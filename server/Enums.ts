const enum GameStateEnum { GameStart = 0, Playing = 1, GameOver = 2 } 
const enum GameBoundEnum { LeftSide = 0, Neither = 1, RightSide = 2 }
const enum PowerStateEnum { Spawned = 0, Activated = 1, Expired = 2 }
const enum PowerGeneratorStateEnum { Off = 0, On = 1 }